N   <- scan(n=1, quiet=TRUE, what=numeric())
i<-1

while (i <= N){
  imp <- scan(n=1, quiet=TRUE, what=numeric())
  dia <- scan(n=1, quiet=TRUE, what=numeric())
  
  if (imp <0){
    
  }
  
  i<- i + 1
}
  
