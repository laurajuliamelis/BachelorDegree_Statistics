
model {

	for ( i in 1:n) {
	
		y[i] ~ dnorm(mu[i], tau[i])
		mu[i] <- b0 + b1*x[i] 
		tau[i] <- tau1*x[i] + tau2*(1-x[i] )

	}

	b0 ~ dnorm(0, 0.001)
	b1 ~ dnorm(0, 0.001)

	tau1 ~ dgamma(0.001, 0.001)
	tau2 ~ dgamma(0.001, 0.001)

}

